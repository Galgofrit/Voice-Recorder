<resources>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="CircleShape">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Explicit sp text size for the Audio/Transcript tabs so they grow with the font-size
         setting (the default Material tab appearance wasn't tracking it). -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/bigger_text_size</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="AppTheme.NoDisplay" parent="AppTheme">
        <item name="android:windowBackground">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoDisplay">true</item>
    </style>

</resources>
